environments:
  - name: dev
    endpoints:
      production:
        url: http://dev.be.com:8595/
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: http://devnow.be.com:8595/
        config:
          retryTimeOut: $RETRY        
  - name: prod
    endpoints:
      production:
        url: http://prod.be.com:8595/
        config:
          retryTimeOut: $RETRY      
      sandbox:
        url: http://prod-now.be.demo.test.com:8595/
        config:
          retryTimeOut: $RETRY 